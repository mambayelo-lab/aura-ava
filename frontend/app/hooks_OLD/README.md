# LEGACY CODE

Ancienne version - Ne pas utiliser
Conservé pour référence uniquement

## Contenu

- `useCanonicalProcess.ts` - Hook pour Process canonique (ancien modèle)
- `useUnifiedInterview.ts` - Hook pour Interview unifié (ancien modèle)

## Migration

Remplacé par le nouveau modèle ontologique dans `lib/types/ontology.ts`.
Les hooks V3 seront créés dans `lib/hooks/` si nécessaire.

